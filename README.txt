This is a basic sort function written in C. It will take in any number of input values, 
from standard in, granted that they are ints. The function will sort the array from 
smallest to largest and then print the sorted array to standard out.
